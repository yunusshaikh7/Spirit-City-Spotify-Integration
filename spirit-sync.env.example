PORT=8012
ClientID=""
ClientSecret=""
SpotifyUser=""
SpotifyDeviceName="Spirit Sync"
SpotifyPlaybackMode="remote"
SpiritSyncExternalUrl="http://127.0.0.1:8012/spirit-sync"

# This bridge uses Spotify PKCE auth, so ClientSecret is accepted for
# compatibility with the old README but is not sent to Spotify.
